# Locust default configuration for Waytoinbox load tests.
# Override any of these on the command line: --users 100 etc.
# https://docs.locust.io/en/stable/configuration.html

locustfile = load_tests/locustfile.py
host = https://staging.waytoinbox.com

# Default headless run: 50 virtual users, ramp up 5/s, run for 5 minutes
# Uncomment to use as a default:
# headless = true
# users = 50
# spawn-rate = 5
# run-time = 5m
# html = load_tests/report.html

# Thresholds that should trigger a CI failure (checked in on_test_stop):
#   - failure rate > 1%
#   - p95 latency  > 3000 ms
